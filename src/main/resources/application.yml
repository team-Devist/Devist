spring:
  profiles:
    active: dev
---

spring:

  profiles: dev
  security:
    user:
      name: user
      password: pwd
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080

---
