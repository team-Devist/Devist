spring:
  profiles:
    active: dev
---

spring:

  profiles: dev
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console
#  mvc:
#    view:
#      prefix: /resources/templates/
#      suffix: .jsp

server:

  port: 8080

---
